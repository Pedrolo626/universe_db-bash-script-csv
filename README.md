# universe_db-bash-script-csv
Relational database modeling celestial bodies using PostgreSQL. The project separates schema and data, providing a schema-only SQL dump, CSV files with astronomical data, and a Bash script to load the data while respecting foreign key relationships
